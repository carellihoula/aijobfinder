# Google sign-in - the Client ID is public (embedded in the built bundle), not
# a secret. Create one at https://console.cloud.google.com/apis/credentials
# ("Create credentials" -> "OAuth client ID" -> "Web application") and add your
# dev/prod origins under "Authorized JavaScript origins".
VITE_GOOGLE_CLIENT_ID=
